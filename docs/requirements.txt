# Sphinx and documentation dependencies
sphinx>=7.1.2
sphinx-rtd-theme>=1.3.0
myst-parser>=3.0.1
sphinx-copybutton>=0.5.0
sphinx-autodoc-typehints>=1.24.0

# Core dependencies needed for autodoc (mocked imports handle the rest)
# These are minimal but may be needed for certain autodoc features
